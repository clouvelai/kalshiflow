[build]
builder = "railpack"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
startCommand = "cd backend && PYTHONPATH=/app/backend/src python -m uvicorn kalshiflow.app:app --host 0.0.0.0 --port $PORT"