[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
startCommand = "uv run uvicorn kalshiflow.app:app --host 0.0.0.0 --port $PORT --ws-ping-interval 30 --ws-ping-timeout 10"