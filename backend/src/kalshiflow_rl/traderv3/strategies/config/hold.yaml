# Hold Strategy Configuration
# No-op baseline strategy that does nothing (for testing/monitoring)

name: hold
enabled: false  # Disabled by default - enable for monitoring-only mode
display_name: "Hold (No Trading)"

# Position limits (not used by hold strategy, but required for consistency)
max_positions: 0
max_exposure_cents: 0
max_trades_per_minute: 0

# No strategy-specific parameters needed
params: {}
